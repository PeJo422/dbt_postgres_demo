version: 2

sources:
  - name: crm_raw  # Name of the source to be referenced in queries
    #database: postgres  # Name of the database
    schema: public # Schema name in the warehouse
    tables: 
      
      - name: transactions
        description: "All transactions processed through the system"

      - name: users
        description: "Registered users with their profiles and preferences"

     
